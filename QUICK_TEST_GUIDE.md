# 🚀 快速测试指南

## 一键运行测试脚本

### 第 1 步：确保开发服务器运行

开发服务器应该已经在后台运行在 http://localhost:1420

如果没有运行，执行：
```bash
cd /Users/zonkidd/IdeaProjects/goal-desk-tauri
nvm use 26
npm run dev
```

### 第 2 步：打开浏览器并执行脚本

1. **打开浏览器**访问：http://localhost:1420

2. **打开开发者工具**
   - macOS: `Cmd + Option + I`
   - Windows/Linux: `F12` 或 `Ctrl + Shift + I`

3. **切换到 Console 标签**

4. **复制粘贴执行脚本**
   - 打开文件：`scripts/seed-browser-data.js`
   - 全选复制：`Cmd+A` 然后 `Cmd+C`
   - 粘贴到控制台：`Cmd+V`
   - 按 `Enter` 执行

5. **点击"确定"刷新页面**

## 🎯 重点测试场景

### 1️⃣ Today 视图 - 今日焦点 ⭐️

**查看今日焦点区域（最重要！）**

进入 Today 视图，你会看到：

```
🔥 今日焦点（6 个任务）

✅ 完成前端界面优化 - 昨天开始，跨天任务
✅ 编写 API 接口文档 - 今天上午 10:00 开始  
✅ 重构支付模块代码 - 前天开始的长期任务
✅ 完成 Rust 第 8 章练习题 - 今天早上 8:00 开始
✅ 阅读《系统设计面试》第 3 章 - 昨晚开始
✅ 本周健身计划执行中 - 本周一开始
```

**为什么这些任务会出现在焦点区域？**
- ✅ 状态是 `IN_PROGRESS`（进行中）
- ✅ 有 `plannedStartAt`（计划开始时间）
- ✅ 今天在任务的时间范围内

**点击任务查看详情：**
- 活动日志显示任务进展
- 可以添加笔记、标记完成等

### 2️⃣ 时间线功能

在 Today 视图下方，查看时间线：

```
⏰ 今日时间线

08:00  完成 Rust 第 8 章练习题
10:00  编写 API 接口文档
19:00  今晚去健身房
20:00  给妈妈打电话
```

### 3️⃣ 注意事项区域

Today 视图会自动分组显示：

- **Overdue** (逾期): 过期但未完成的任务
- **Due Today** (今天截止): 今天必须完成的任务
- **Ongoing** (进行中): 正在推进的任务

### 4️⃣ Inbox 视图 - 收件箱

查看所有待办任务：
- 按 Goal 分组的任务
- 无 Goal 的收件箱任务
- 可以快速添加新任务

### 5️⃣ Goals 视图 - 目标看板

12 个目标卡片：
- 进度条展示（35%, 45%, 90% 等）
- 点击查看关联的任务
- 不同状态标签（Active/Paused/Ready to Complete）

### 6️⃣ Areas 视图 - 领域管理

5 个领域卡片：
- 工作、个人成长、健康、家庭、财务
- 每个领域显示目标统计
- 点击"打开看板"进入该领域

## 📊 预期效果

运行脚本后，你将拥有：

- ✅ **5 个领域**
- ✅ **12 个目标**（不同状态和进度）
- ✅ **25 个任务**（16 个待办 + 6 个进行中 + 3 个已完成）
- ✅ **6 个今日焦点任务**（最重要的展示）
- ✅ **完整的活动日志**
- ✅ **真实的时间规划**

## 🔄 数据持久化验证

1. 修改任何任务的状态（标记完成、添加笔记等）
2. 创建新的任务或目标
3. **刷新页面**（`F5` 或 `Cmd+R`）
4. ✅ 验证：所有更改都被保存！

这就是 localStorage 持久化的效果！

## 🧹 清空数据重新测试

在浏览器控制台执行：

```javascript
localStorage.clear()
location.reload()
```

然后重新运行种子数据脚本。

## 💡 交互测试建议

1. **点击今日焦点任务**
   - 查看任务详情和活动日志
   - 添加新的笔记
   - 标记任务完成

2. **修改任务状态**
   - 将 TODO 任务标记为 "开始"
   - 查看是否出现在今日焦点区域

3. **创建新任务**
   - 在 Inbox 创建任务
   - 设置计划开始时间为今天
   - 点击"开始"按钮
   - 查看是否出现在 Today 视图的焦点区域

4. **测试 Goal-Task 关联**
   - 打开任意目标详情
   - 查看关联的任务列表
   - 添加新任务到该目标

5. **测试时间线**
   - 查看 Today 视图的时间线
   - 确认任务按时间排序
   - 点击时间线项目打开详情

## 🎉 享受测试！

这个脚本包含了完整的真实场景数据，能够全面展示 Goal Desk 的各项功能，特别是**今日焦点**这个核心功能！
